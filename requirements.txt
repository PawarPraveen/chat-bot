# --- Core agent framework ---
langchain>=0.3.0
langchain-core>=0.3.0

# --- LLM backends (only the one matching your BACKEND setting is strictly
#     required at runtime, but installing all three lets you switch freely) ---
langchain-openai>=0.2.0      # BACKEND = "openai"  (gpt-4o-mini)
langchain-groq>=0.2.0        # BACKEND = "groq"    (free tier, Llama 3.3 70B)
langchain-ollama>=0.2.0      # BACKEND = "ollama"  (fully local)

# --- Document parsing (layout-aware, local) ---
unstructured[pdf,docx]>=0.16.0

# --- Embeddings for retrieval ---
sentence-transformers>=3.0.0
numpy>=1.26.0